🚀 VERCEL'E DEPLOY ETME - HIZLI REHBERİ
==========================================

✅ HAZIRLIK TAMAMLANDI:
- Build başarılı: npm run build ✓
- vercel.json yapılandırması ✓
- .gitignore dosyası ✓
- Environment variable desteği ✓

📋 ADIMLAR:

1. GIT BAŞLAT VE GITHUB'A YÜKLE
   --------------------------------
   git init
   git add .
   git commit -m "Deprem Takip Uygulaması"
   
   # GitHub'da yeni repo oluştur, sonra:
   git remote add origin https://github.com/KULLANICI_ADINIZ/deprem-takip.git
   git branch -M main
   git push -u origin main

2. VERCEL'E DEPLOY
   ----------------
   A) Web Üzerinden (Kolay):
      - vercel.com adresine git
      - "Add New Project" tıkla
      - GitHub repo'nu seç
      - Framework: Vite
      - Build Command: npm run build
      - Output Directory: dist
      - Deploy'a tıkla

   B) CLI ile:
      npm i -g vercel
      vercel
      vercel --prod

3. BACKEND İÇİN (Opsiyonel - Anlık Bildirimler için)
   ---------------------------------------------------
   WebSocket için backend'i ayrı deploy et:
   
   Railway.app (Önerilen):
   - railway.app'e git
   - "New Project" > "Deploy from GitHub"
   - server klasörünü seç
   - Deploy et
   - URL'yi kopyala

   Vercel'de Environment Variable ekle:
   - VITE_SOCKET_URL = https://your-backend-url.railway.app

⚠️ ÖNEMLİ NOTLAR:

1. WebSocket olmadan da çalışır (fallback modu)
2. PWA için HTTPS gerekli (Vercel otomatik sağlar)
3. Build dosyaları dist/ klasöründe
4. Her git push otomatik deploy tetikler

📱 SONUÇ:
- Frontend: https://your-app.vercel.app
- PWA olarak kurulabilir
- Mobil uyumlu
- Otomatik SSL/HTTPS

🔧 SORUN GİDERME:
- Build hatası: node_modules sil, npm install
- WebSocket çalışmıyor: Normal, fallback modu devrede
- PWA çalışmıyor: HTTPS gerekli (Vercel'de otomatik)

📚 DETAYLI REHBERİ DEPLOY.md DOSYASINDA BULABILIRSINIZ
